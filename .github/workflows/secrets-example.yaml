name: Secrets example
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run: |
          if [ "$SECRET_TEST" == "test" ]
          then
            echo "Got secret"
          fi
