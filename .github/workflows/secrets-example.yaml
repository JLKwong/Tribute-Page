name: Secrets example
on: push
jobs:
  job1:
    runs-on: ubuntu-latest
    steps:
      - name: Test
        run:
          if [ "${{ secrets.CANDY_SECURE }}" == "candy" ]
          then
            echo "Got secret"
          fi
